[project]
name = "scripts"
version = "0.1.0"
description = "Tools and experiments"
requires-python = ">=3.13,<3.14"
dependencies = [
    "numpy>=2.4.6",
    "tensorflow>=2.21.0",
]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
    "ruff>=0.15.15",
]
